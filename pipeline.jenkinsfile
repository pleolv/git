pipeline {
  agent none
  stages {
    stage('Poczatek') {
      sh 'echo $user'
      // cos tutaj bedzie )
  }
  stage('Testuje') {
    // co nie zrobisz dla nauki )
    sh 'ansible all -m ping'
    sh 'ls -la'
  }
  agent any
  stage('Wkoncu release') {
    echo "Serwer dziala :"
    sh 'ansible all -a "uptime"'
  }
}
