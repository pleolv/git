pipeline {
  agent any
  stage('Poczatek') {
    sh 'echo $user'
   // cos tutaj bedzie )
  }
  stage('Testuje') {
    // co nie zrobisz dla nauki )
    sh 'ansible all -m ping'
    sh 'ls -la'
  }
  stage('release') {
    echo "Serwer dziala :"
    sh 'ansible all -a "uptime"'
  }
}
